this will be the project structure of a flow project.

please put notes here so i can see them in future pushes.


this is the structiure im striving for:

[dirfolder]
    -[name].fc (flow config)
    -[name].fb (flow build)
    -[name].fpr (flow project)
    [name](optional folder)
        -main.flo (expected entry)
